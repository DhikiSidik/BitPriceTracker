import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titte: {
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold'
    },
    ticker: {
        color: 'grey',
        fontWeight: '600',
    },
    container: {
        flexDirection: 'row',
        padding: 15,
    },
    quanti: {
        marginLeft: 'auto',
        alignItems: 'flex-end',
    }
});

export default styles;